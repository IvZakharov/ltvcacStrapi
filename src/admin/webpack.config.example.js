export default {
  config: {
    // Replace the Strapi logo in auth (login) views

    // Override or extend the theme
    theme: {
      colors: {
        primary100: '#000',
        primary200: '#000',
        primary500: '#000',
        primary600: '#000',
        primary700: '#000',
        danger700: '#000'
      },
    },
    // Extend the translations

  },

  bootstrap() {},
};

